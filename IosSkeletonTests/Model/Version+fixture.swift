@testable import IosSkeleton
import Foundation

extension Version {
    static func fixture(
        version: String = ""
    ) -> Version {
        Version(
            version: version
        )
    }
}
